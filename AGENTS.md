# Flash Drum — Agent Guide

> **➡️ The canonical guide for ALL AI agents (Claude, Codex, Kimi, …) is [`CLAUDE.md`](./CLAUDE.md) at the repo root.**
>
> Read `CLAUDE.md` before any non-trivial change. It is the single source of
> truth for architecture, invariants, the build/test/deploy workflow, the
> mandatory "À tester dans Studio One" checklist rule, and the "next" /
> "on continue" workflow rule.
>
> This file used to duplicate that content and drifted out of date; it now just
> redirects so there is one place to keep current.

## Quick pointers

- **Architecture, invariants, build & deploy rules** → `CLAUDE.md`
- **Adding a synthesis voice** → `ADDING_AN_INSTRUMENT.md`
- **Active priorities / known issues** → `TODO.md`
- **Build history (one entry per installed build)** → `CHANGELOG.md`
- **UI redesign scope & pitfalls (read before touching `src/ui/`)** → `docs/design/UI-REDESIGN-HANDOFF.md`
- **Handoff / current session state** → `docs/HANDOFF.md`
- Older notes under `docs/historique/` are historical references, not the active source of truth.
